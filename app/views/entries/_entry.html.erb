<li>
  <span class="content"> 
  <h5>Title: <%= link_to entry.title, entry %></h5>
  </span> 
  <span class="content"> 
  <h5>Content: </h5>
  <%=  truncate(entry.content,length: 55, separator: ' ') %></span>

  <span class="timestamp">
    Posted <%= time_ago_in_words(entry.created_at) %> ago. / at <%=time_tag(entry.created_at)%> 
  </span>
</li>